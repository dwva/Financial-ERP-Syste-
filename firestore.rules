rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all users to read and write all documents for testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}